import { styled } from "@mui/material"

const LabelStyles = styled("label")(() => ({
    padding: "0 0 4px",
    lineHeight: 1.5,
    display: "block",
    textAlign: "left",
    fontFamily: "Roboto",
    color: "#7F8899",
}))

export default LabelStyles